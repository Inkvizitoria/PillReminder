<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/calendarCellNoOffset"
    android:layout_width="175dp"
    android:layout_height="175dp"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="4dp"
    android:background="@drawable/day_block_background">

    <!-- Контейнер для номера дня и дня недели в одной строке -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <!-- Номер дня -->
        <TextView
            android:id="@+id/tvDayNumber"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="1"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginEnd="6dp"/>

        <!-- День недели (например, Пн) -->
        <TextView
            android:id="@+id/tvDayOfWeek"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Пн"
            android:textSize="14sp"/>
    </LinearLayout>

    <!-- Контейнер для "прямоугольников" (блоков записей) -->
    <LinearLayout
        android:id="@+id/dayEventContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_vertical"
        android:layout_marginVertical="8dp"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"/>
</LinearLayout>
