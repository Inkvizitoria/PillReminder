<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Прозрачный фон -->
    <solid android:color="@android:color/transparent"/>

    <!-- Толстая контрастная обводка (8dp) -->
    <stroke
        android:width="4dp"
        android:color="@android:color/black"/>
</shape>
